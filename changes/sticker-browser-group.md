# Sticker browser group

## Fixed

- Exposed the Video Editor sticker browser as a named group so its Stickers label reaches assistive technology instead of being silently ignored on a role-less container.
