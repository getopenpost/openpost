# Repository Map

Use this map to pull only the context needed for the current ticket. Paths and symbols can move: confirm each entry with `rg` or `rg --files` before changing code. Recall `project:openpost` from Hindsight for domain terms and prior decisions, then verify them against current code and the surrounding versioned design/spec documents.

| Area                                          | Read first                                                                                  | Follow the seam                                                                                                                                                               | Targeted verification                                                                                 |
| --------------------------------------------- | ------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| Publication and Rendition authoring/lifecycle | `backend/internal/models/models.go` (`Publication`, `Rendition`)                            | `backend/internal/services/lifecycle/`, `backend/internal/api/handlers/publications.go`, then `frontend/src/routes/publications/` and the text-and-thread composer            | `bun run test -- backend`, `bun run check -- frontend`, `bun run test -- frontend`                    |
| Provider adapters and capabilities            | `backend/internal/platform/adapter.go` (`Adapter` and optional capability interfaces)       | The provider file in `backend/internal/platform/`, `backend/internal/capabilities/`, and provider policy/readiness services                                                   | `bun run test -- backend`, `bun run lint -- backend`                                                  |
| Durable jobs                                  | `backend/internal/models/models.go` (`Job`) and `backend/internal/jobregistry/`             | `backend/internal/queue/`, job producers in services/handlers, then migrations when persistence changes                                                                       | `bun run test -- backend`, `bun run lint -- backend`                                                  |
| Media, storage, and editors                   | `backend/internal/services/mediastore/local.go` (`BlobStorage`)                             | Media services and handlers, `frontend/src/lib/{image-editor,video-editor,quick-cut}/`, and `frontend/src/routes/{media,editors,image-editor,video-editor,quick-cut,record}/` | `bun run test -- backend`, `bun run check -- frontend`, `bun run test -- frontend`                    |
| Auth, workspace, and billing                  | `backend/internal/services/{auth,workspaceaccess,billing}/`                                 | Related middleware/handlers and `frontend/src/routes/{login,onboarding,settings,checkout}/`                                                                                   | `bun run test -- backend`, `bun run check -- frontend`, `bun run test -- frontend`                    |
| MCP and CLI                                   | `backend/internal/api/handlers/mcp.go` (`mcpOperationCatalog`) and `cli/internal/commands/` | `cli/internal/api/client.go`, MCP routing tests, and generated CLI/OpenAPI references                                                                                         | `bun run test -- backend`, `bun run test -- cli`, `bun run lint -- cli`, `bun run check -- contracts` |
| Frontend app and shared UI                    | `frontend/src/routes/` for the owning route                                                 | `frontend/src/lib/components/`, especially `ui/`, plus typed API helpers under `frontend/src/lib/`                                                                            | `bun run check -- frontend`, `bun run test -- frontend`, `bun run check -- ui-consistency`            |
| Native mobile app                             | `mobile/README.md`, then the owning route under `mobile/src/app/`                           | Shared controls in `mobile/src/components/`, typed API helpers in `mobile/src/lib/`, and persistent native config in `mobile/app.json` or `mobile/plugins/`                   | `cd mobile && bun run check`, then `bun run build:android:apk` for device proof                       |
| Marketing and documentation                   | `marketing-site/src/routes/` or the owning `docs-site/` section                             | Shared packages and the asset surface manifest when claims or assets cross surfaces                                                                                           | `bun run build -- marketing`, `bun run build -- docs`, `bun run check -- docs`                        |
| Generated contracts                           | The source Huma route, CLI command, message catalog, or manifest                            | `scripts/check-contracts.mjs`, generated OpenAPI/TypeScript/CLI-doc boundaries; change the source, never the generated output alone                                           | `bun run check -- contracts`, `bun run check -- frontend`                                             |
| Project verification                          | `devenv.nix`, `devenv.yaml`, root `package.json`, and `turbo.json`                          | The root task dispatcher and the closest existing test file before broad gates                                                                                                | scoped root `check`, `lint`, `test`, or `build`, then `bun run verify` in proportion to risk          |

Run repository gates from the root with `bun run <gate> -- <scope>`. Omit the scope for the complete repository gate.

Before changing runtime configuration, release workflows, or revision evidence, read `docs/agents/deployable-inventory.md` for each deployable's owner, release path, and proof boundary.
