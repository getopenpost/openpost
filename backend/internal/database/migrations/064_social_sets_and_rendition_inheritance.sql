-- 064: Restore format-independent Social Sets and explicit rendition inheritance.
--
-- The migration needs to support the repository's intentionally partial legacy
-- schemas. Its conditional DDL and backfill live in prepareMigration.
SELECT 1;
