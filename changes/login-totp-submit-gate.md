### Fixed

- The login verification code button now stays disabled until a full six-digit authenticator code is entered, instead of letting incomplete codes be submitted for a round-trip that always fails.
