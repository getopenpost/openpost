### Fixed

- Media search fields in the library page and the media picker now expose an explicit accessible name, instead of relying on the placeholder alone so screen-reader users keep the label while typing.
