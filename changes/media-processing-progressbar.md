# media-processing-progressbar

## Fixed

- The media library video-processing indicator is now exposed as a named
  progress bar with its current percent value, so assistive technology users
  get programmatic progress semantics (previously a plain visual bar) alongside
  the existing polite live-region status text.
