### Fixed

- Video Editor scene browser and keyboard shortcut search fields now expose an explicit accessible name, instead of relying on the placeholder alone so screen-reader users keep the label while typing.
