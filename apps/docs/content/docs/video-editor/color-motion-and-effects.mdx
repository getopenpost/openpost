---
title: Color, motion, and effects
description: Change the look of a clip and animate visual layers.
icon: WandSparkles
---

import { SetupScreenshot } from "@/components/setup-screenshot";

Use the dedicated workspace for the kind of change you are making. This keeps grading controls, keyframes, and the normal clip inspector from competing for the same space.

## Adjust color

Open **Color**, then select the clip or sequence grade you want to change. Scopes show the balance of light and color. Primaries, wheels, and curves control the grade. You can also sample a neutral, black, or white point from the current preview frame.

<SetupScreenshot
  src="/assets/screenshots/video-color-light.webp"
  darkSrc="/assets/screenshots/video-color-dark.webp"
  alt="Color workspace with scopes, grade presets, color wheels, and curves"
  width={1440}
  height={960}
  caption="Scopes, presets, wheels, and curves in the Color workspace. Screenshot uses example media."
/>

Use **Before**, **After**, or **Split** to compare the result. Copy and paste a grade between clips, save a grade preset, or use one sequence grade when the whole edit needs the same finish.

## Animate a layer

Open **Motion** to animate text, shapes, images, video, and supported effect controls. Create or open a composition, set a keyframe, move the playhead, then change the value. The Dope Sheet shows timing and the Graph view controls easing.

<SetupScreenshot
  src="/assets/screenshots/video-motion-light.webp"
  darkSrc="/assets/screenshots/video-motion-dark.webp"
  alt="Motion workspace with a composition setup beside the video preview"
  width={1440}
  height={960}
  caption="The Motion workspace before adding a composition. Screenshot uses example media."
/>

Example: to slide a title in, place it off screen at the first position keyframe. Move one second forward, place it at rest, then play the range and adjust the easing.

## Add effects and transitions

Open **Effects** in the left panel. Select a clip, then add a preset, or drag it onto one or more timeline clips. The inspector lets you reorder, disable, remove, reset, and keyframe supported effect parameters. You can save the current stack as your own preset.

<SetupScreenshot
  src="/assets/screenshots/video-effects-light.webp"
  darkSrc="/assets/screenshots/video-effects-dark.webp"
  alt="Effects browser with visual presets beside the preview and inspector"
  width={1440}
  height={960}
  caption="The Effects browser beside the preview. Screenshot uses example media."
/>

Use an adjustment layer when one effect or grade should cover several clips below it. Add a transition only after checking the cut without it, then play the whole transition before export.
