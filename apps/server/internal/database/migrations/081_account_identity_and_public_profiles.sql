-- The cross-dialect account identity upgrade is prepared and repaired by
-- ensureAccountIdentityPublicProfileSchema. Historical-schema fixtures may
-- intentionally omit OIDC tables, so the recorded SQL stays side-effect free.
SELECT 1;
