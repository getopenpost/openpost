---
title: Inbox and notifications
description: Read comments, reply to conversations, and choose the alerts you receive.
icon: Inbox
---

Comments and replies, Direct messages, and notifications have different controls. Enable the account features you need, then use the Inbox to review saved activity.

## Enable comments and messages

Open **Settings → Workspace → Social accounts**, select an account, and inspect **Comments and replies** and **Direct messages**. Enable each supported feature separately. A provider may support comments without supporting direct messages, or require additional authorization before an option becomes available.

Turning a feature off stops its future activity. Saved history stays available and the social account remains connected.

## Reply to a comment

Open the engagement list in **Inbox**, filter by account or publication, and select the comment. Write a reply and send it. OpenPost shows the delivery state; if it fails, inspect the error before retrying.

Moderation actions depend on the provider. Use the actions shown for that comment to hide, delete, or respond. Use **Load older** to reach saved history beyond the first page.

## Work with direct messages

Choose a conversation to read its newest saved messages. Scroll upward or choose **Load older messages** for earlier history. Filters and the selected workspace determine which conversations you see.

Check the conversation's provider restrictions before replying:

- Facebook and Instagram enforce a customer-service reply window. OpenPost blocks sends after the stored deadline.
- X requires direct-message permission for the app and connected account.
- Bluesky chat requires an app-password session.
- Mastodon direct posts are visible to the mentioned accounts and involved servers. They are not end-to-end encrypted.

A failed reply stays visible with its status. Reconnect an expired account or fix the reported issue before retrying. Sending a message does not create a publication.

## Choose notifications

Open **Settings → Personal → Notifications**. For each optional event, choose **Off**, **Immediate**, or **Daily email**. Set a delivery time and timezone for the daily digest. If email delivery is unavailable, a self-hosted administrator must configure an [email provider](/self-hosting/email).

In-app notifications remain immediate. Security, access, invitations, and critical billing notices keep their transactional delivery rules.

Use a temporary **Mute** to pause optional email for every workspace or one selected workspace until a chosen time. **End now** restores your saved choices early. Muting does not disable in-app alerts or transactional messages.

Notifications for a publication link to the affected result. If only one destination failed, inspect that rendition and retry it without reposting to the destinations that succeeded.
