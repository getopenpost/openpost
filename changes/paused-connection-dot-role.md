# Paused connection dot accessible name

## Fixed

- Exposed the paused-connection indicator dot on inactive social accounts as a
  labeled image so its "Connection paused" description reaches assistive
  technology instead of being silently ignored on a role-less span.
