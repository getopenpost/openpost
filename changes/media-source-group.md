# Media source switcher group

## Fixed

- Exposed the media picker source switcher (Library, Device, Camera, and creation
  actions) as a named group so assistive technology announces its "Source" label
  instead of silently ignoring it.
