# Linked sync badge accessible name

## Fixed

- Exposed the Video Editor linked A/V drift badge as a labeled image so its
  "Linked clips are out of sync by ..." description reaches assistive
  technology instead of being silently ignored on a role-less span.
