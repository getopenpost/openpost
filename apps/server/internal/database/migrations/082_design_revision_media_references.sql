-- The cross-dialect revision-media ownership tables are prepared by
-- ensureDesignRevisionMediaReferenceSchema. Historical fixtures may
-- intentionally omit the Image Editor tables, so the recorded SQL stays
-- side-effect free.
SELECT 1;
