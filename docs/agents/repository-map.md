# Repository Map

Use this map to pull only the context needed for the current ticket. Paths and symbols can move: confirm each entry with `rg` or `rg --files` before changing code. Recall `project:openpost` from Hindsight for domain terms and prior decisions, then verify them against current code and the surrounding versioned design/spec documents.

The root groups projects by purpose:

- `apps/`: `web`, `server`, `mobile`, `cli`, `marketing`, and `docs`.
- `packages/`: shared libraries and the published n8n connector.
- `tests/`: application and marketing browser suites.
- `deploy/`: container image and Cloudflare configuration. Root `docker-compose.yml` keeps default Compose discovery.
- `config/`: compatibility, release ownership, MCP registry, provider certification, and Knip configuration.
- `docs/`: engineering docs, examples, and ignored reference checkouts.
- `assets/`: canonical shipped artwork and maintained design sources.
- `scripts/`: build, release, validation, and lint tooling.

Root task scopes remain `frontend`, `backend`, `cli`, `marketing`, and `docs`. Go module identities remain unchanged for external consumers. The native app keeps its own dependency lock and commands under `apps/mobile/`.

| Area                                          | Read first                                                                                           | Follow the seam                                                                                                                                                                 | Targeted verification                                                                                 |
| --------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| Publication and Rendition authoring/lifecycle | `apps/server/internal/models/models.go` (`Publication`, `Rendition`)                                 | `apps/server/internal/services/lifecycle/`, `apps/server/internal/api/handlers/publications.go`, then `apps/web/src/routes/publications/` and the text-and-thread composer      | `bun run test -- backend`, `bun run check -- frontend`, `bun run test -- frontend`                    |
| Provider adapters and capabilities            | `apps/server/internal/platform/adapter.go` (`Adapter` and optional capability interfaces)            | The provider file in `apps/server/internal/platform/`, `apps/server/internal/capabilities/`, and provider policy/readiness services                                             | `bun run test -- backend`, `bun run lint -- backend`                                                  |
| Durable jobs                                  | `apps/server/internal/models/models.go` (`Job`) and `apps/server/internal/jobregistry/`              | `apps/server/internal/queue/`, job producers in services/handlers, then migrations when persistence changes                                                                     | `bun run test -- backend`, `bun run lint -- backend`                                                  |
| Media, storage, and editors                   | `apps/server/internal/services/mediastore/local.go` (`BlobStorage`)                                  | Media services and handlers, `apps/web/src/lib/{image-editor,video-editor,quick-cut}/`, and `apps/web/src/routes/{media,editors,image-editor,video-editor,quick-cut,record}/`   | `bun run test -- backend`, `bun run check -- frontend`, `bun run test -- frontend`                    |
| Auth, workspace, and billing                  | `apps/server/internal/services/{auth,workspaceaccess,billing}/`                                      | Related middleware/handlers and `apps/web/src/routes/{login,onboarding,settings,checkout}/`                                                                                     | `bun run test -- backend`, `bun run check -- frontend`, `bun run test -- frontend`                    |
| MCP and CLI                                   | `apps/server/internal/api/handlers/mcp.go` (`mcpOperationCatalog`) and `apps/cli/internal/commands/` | `apps/cli/internal/api/client.go`, MCP routing tests, and generated CLI/OpenAPI references                                                                                      | `bun run test -- backend`, `bun run test -- cli`, `bun run lint -- cli`, `bun run check -- contracts` |
| Frontend app and shared UI                    | `apps/web/src/routes/` for the owning route                                                          | `apps/web/src/lib/components/`, especially `ui/`, plus typed API helpers under `apps/web/src/lib/`                                                                              | `bun run check -- frontend`, `bun run test -- frontend`, `bun run check -- ui-consistency`            |
| Native mobile app                             | `apps/mobile/README.md`, then the owning route under `apps/mobile/src/app/`                          | Shared controls in `apps/mobile/src/components/`, typed API helpers in `apps/mobile/src/lib/`, and persistent native config in `apps/mobile/app.json` or `apps/mobile/plugins/` | `cd apps/mobile && bun run check`, then `bun run build:android:apk` for device proof                  |
| Marketing and documentation                   | `apps/marketing/src/routes/` or the owning `apps/docs/` section                                      | Shared packages and the asset surface manifest when claims or assets cross surfaces                                                                                             | `bun run build -- marketing`, `bun run build -- docs`, `bun run check -- docs`                        |
| Generated contracts                           | The source Huma route, CLI command, message catalog, or manifest                                     | `scripts/check-contracts.mjs`, generated OpenAPI/TypeScript/CLI-doc boundaries; change the source, never the generated output alone                                             | `bun run check -- contracts`, `bun run check -- frontend`                                             |
| Project verification                          | `devenv.nix`, `devenv.yaml`, root `package.json`, and `turbo.json`                                   | The root task dispatcher and the closest existing test file before broad gates                                                                                                  | scoped root `check`, `lint`, `test`, or `build`, then `bun run verify` in proportion to risk          |

For release workflow changes, run `bun run check -- workflows` and `bun run check -- release-version`. Knip production patterns in `config/knip.jsonc` must end in `!`; keep build-generated asset inputs explicit and test fixtures outside the production graph.

For agent instructions or workflow tooling, read `AGENTS.md`, `.agents/skills/agent-workflow/SKILL.md`, and `scripts/agent-doctor.mjs`; verify with `bun run check -- agents`. The default checker validates repository-owned files and the local label table without network access. See `docs/agents/triage-labels.md` for the explicit live GitHub label check. Global skills are supplied by the agent environment.

Run repository gates from the root with `bun run <gate> -- <scope>`. Omit the scope for the complete repository gate.

Before changing runtime configuration, release workflows, or revision evidence, read `docs/agents/deployable-inventory.md` for each deployable's owner, release path, and proof boundary.
