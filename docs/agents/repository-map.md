# Repository Map

Use this map to pull only the context needed for the current ticket. Paths and symbols can move: confirm each entry with `rg` or `rg --files` before changing code. Read `CONTEXT.md` first for domain terms and check the surrounding `docs/` tree for relevant ADRs or design decisions.

| Area                                          | Read first                                                                                  | Follow the seam                                                                                                                                                            | Targeted verification                                                                                  |
| --------------------------------------------- | ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| Publication and Rendition authoring/lifecycle | `backend/internal/models/models.go` (`Publication`, `Rendition`)                            | `backend/internal/services/lifecycle/`, `backend/internal/api/handlers/publications.go`, then `frontend/src/routes/publications/` and the text-and-thread composer         | `backend-test`, `frontend-check`, `frontend-test`                                                      |
| Provider adapters and capabilities            | `backend/internal/platform/adapter.go` (`Adapter` and optional capability interfaces)       | The provider file in `backend/internal/platform/`, `backend/internal/capabilities/`, and provider policy/readiness services                                                | `backend-test`, `backend-lint`                                                                         |
| Durable jobs                                  | `backend/internal/models/models.go` (`Job`) and `backend/internal/jobregistry/`             | `backend/internal/queue/`, job producers in services/handlers, then migrations when persistence changes                                                                    | `backend-test`, `backend-lint`                                                                         |
| Media, storage, and studio                    | `backend/internal/services/mediastore/local.go` (`BlobStorage`)                             | Media services and handlers, `backend/internal/videoproject/`, `packages/video-project/`, and `frontend/src/routes/{media,studio,image-editor,video-editor,video-studio}/` | `backend-test`, `frontend-check`, `frontend-test`                                                      |
| Auth, workspace, and billing                  | `backend/internal/services/{auth,workspaceaccess,billing}/`                                 | Related middleware/handlers and `frontend/src/routes/{login,onboarding,settings,checkout}/`                                                                                | `backend-test`, `frontend-check`, `frontend-test`                                                      |
| MCP and CLI                                   | `backend/internal/api/handlers/mcp.go` (`mcpOperationCatalog`) and `cli/internal/commands/` | `cli/internal/api/client.go`, MCP routing tests, and generated CLI/OpenAPI references                                                                                      | `backend-test`, `(cd cli && go test ./...)`, `(cd cli && golangci-lint run)`, `check:contracts`        |
| Frontend app and shared UI                    | `frontend/src/routes/` for the owning route                                                 | `frontend/src/lib/components/`, especially `ui/`, plus typed API helpers under `frontend/src/lib/`                                                                         | `frontend-check`, `frontend-test`, `check:ui-consistency`                                              |
| Marketing and documentation                   | `marketing-site/src/routes/` or the owning `docs-site/` section                             | Shared packages and the asset surface manifest when claims or assets cross surfaces                                                                                        | `bun run marketing:build`, `docs-build`, `check:docs`                                                  |
| Generated contracts                           | The source Huma route, CLI command, message catalog, or manifest                            | `scripts/check-contracts.mjs`, generated OpenAPI/TypeScript/CLI-doc boundaries; change the source, never the generated output alone                                        | `check:contracts`, `frontend-check`                                                                    |
| Project verification                          | `devenv.nix`, `devenv.yaml`, root `package.json`, and `turbo.json`                          | Package-owned scripts and the closest existing test file before broad gates                                                                                                | targeted `*-check`/`*-test`/`*-lint`, then `check`, `test`, `build`, or `verify` in proportion to risk |

Run named Devenv commands directly after direnv loads; otherwise use `devenv shell -- <command>`. For root Bun scripts such as `check:contracts`, use `bun run <command>`.
