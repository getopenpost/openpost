# Quick Cut back button accessible name

## Fixed

- The Quick Cut editor header back button now announces "Back" instead of the
  application title, so assistive technology describes the control's action
  (return to the project start screen, confirming first when unsaved local
  work would be discarded) instead of mislabeling it. This matches the
  image-editor back button pattern.
