# Deployable Inventory

Read this inventory before changing runtime configuration, release workflows, or revision evidence. A successful build proves only that an artifact was built. Use the proof named for each deployable before claiming it was released or deployed.

| Deployable                  | Configuration owner                                                                                                                                                                                                                                                                                                                                                                                                    | Release path                                                                                                                                                                                                                                                                                                                                                                                                                                     | Revision proof                                                                                                                                                                                                                                                                                                                                                 |
| --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Server and embedded web app | Bootstrap and data-plane values come from environment variables or their `_FILE` forms through `apps/server/internal/config/config.go`. Admin-managed settings are declared in `apps/server/internal/config/managed.go` and stored through `apps/server/internal/services/instancesettings/service.go`.                                                                                                                | Tag CI forces the full core gates for the tagged SHA and builds/scans `deploy/docker/Dockerfile` with the final version embedded, retaining its registry digest as an Actions artifact. The release workflow promotes that tested digest, deploys it, and uploads `openpost-server-*` binaries built from the tagged SHA.                                                                                                                        | The release workflow downloads the digest from successful tag CI and checks the image OCI version and revision labels. The image smoke test and production check require matching `/api/v1/version` and ready `/api/v1/ready`. Standalone binaries embed the tag and SHA.                                                                                      |
| Marketing site              | Source and build configuration live under `apps/marketing/`, including `apps/marketing/vite.config.ts`. The external runtime owner is the Cloudflare Pages project `openpost-marketing`.                                                                                                                                                                                                                               | The Pages Git integration builds clean `main` to `apps/marketing/dist`. CI retains a build artifact for verification but does not deploy it.                                                                                                                                                                                                                                                                                                     | Run the marketing build, then `scripts/public-deployment-proof.mjs prove` as described in `docs/operations/cloudflare-edge-plan.md`. It requires the local full SHA, the Pages deployment full SHA, and the live host to agree.                                                                                                                                |
| Documentation site          | Source and build configuration live under `apps/docs/`, with authored pages in `apps/docs/content/docs/`. The external runtime owner is the Cloudflare Pages project `openpost-docs`.                                                                                                                                                                                                                                  | The Pages Git integration builds clean `main` to `apps/docs/out`. CI retains a build artifact for verification but does not deploy it.                                                                                                                                                                                                                                                                                                           | Run the documentation build, then the same public deployment proof. It requires the local full SHA, the Pages deployment full SHA, and the live host to agree.                                                                                                                                                                                                 |
| Android app                 | Release name and code live in `apps/mobile/app.json` and `apps/mobile/package.json` and advance through explicit mobile commits, never through core release preparation. Native persistence belongs in `apps/mobile/app.json` or `apps/mobile/plugins/`. Per-install server and token state live in `apps/mobile/src/lib/server.ts` and `apps/mobile/src/lib/api/token-store.ts`. GitHub owns release signing secrets. | Tag CI builds the unsigned APK only when the tag diff touches a mobile surface. The release workflow requires the committed identity to stay at the released identity for server-only releases (or advance monotonically for mobile releases), downloads the APK from successful tag CI, verifies the checksum, signs it, and uploads `openpost-app-android.apk`. Server-only releases verify and publish core assets without Android packaging. | `scripts/mobile-release.mjs check-release` enforces the conditional identity rule; `check-current` guards everyday CI. The checksum verifies the unsigned artifact; `apksigner verify` verifies the signed APK. The source revision belongs to the CI run.                                                                                                     |
| CLI and MCP proxy           | Profiles and endpoint precedence live in `apps/cli/internal/config/config.go`. Tokens live behind `apps/cli/internal/auth/token_store.go`, never in the profile file.                                                                                                                                                                                                                                                  | The tag workflow builds `openpost-cli-*` and `openpost-mcp-*` for the release matrix and uploads them to the GitHub release.                                                                                                                                                                                                                                                                                                                     | The binaries embed and print the release tag. The GitHub release tag identifies the source SHA. Each binary ships a detached `.sha256` checksum consumed by the npm wrapper; the binaries still do not embed the full SHA. Do not claim stronger executable-level revision proof.                                                                              |
| n8n package                 | Package identity and version live in `packages/n8n-nodes-openpost/package.json`. User-entered endpoint and token fields live in `packages/n8n-nodes-openpost/credentials/OpenPostApi.credentials.ts`. npm trusted-publisher configuration is external.                                                                                                                                                                 | The tag workflow packs and publishes `@getopenpost/n8n-nodes-openpost` through trusted publishing beside the Hosted deployment, never behind it. Follow `docs/agents/n8n-package-release.md`.                                                                                                                                                                                                                                                    | `scripts/n8n-package-release.mjs` matches the packed tarball integrity to npm. `scripts/verify-published-n8n-package.mjs` checks registry integrity, npm signatures, and a clean install. npm provenance records the workflow source. The package SemVer is independent of the app tag.                                                                        |
| TypeScript SDK              | Package identity and version live in `packages/sdk/package.json`. Token auth and base URL come from constructor options or `OPENPOST_TOKEN`/`OPENPOST_URL`; the default workspace resolves from `OPENPOST_WORKSPACE_ID`/`OPENPOST_WORKSPACE`. npm trusted-publisher configuration is external.                                                                                                                         | The tag workflow packs and publishes `@getopenpost/sdk` through trusted publishing beside the Hosted deployment, never behind it. Follow `docs/agents/npm-packages.md`.                                                                                                                                                                                                                                                                          | `scripts/npm-package-release.mjs` matches the packed tarball integrity to npm. `scripts/verify-published-npm-package.mjs --kind sdk` checks registry integrity, npm signatures, a clean install, and the exported client surface. npm provenance records the workflow source. The package SemVer is independent of the app tag.                                |
| npm CLI wrapper             | Package identity and version live in `packages/cli/package.json`; the default GitHub release tag lives under its `openpost.releaseTag` key. Cache and overrides come from `OPENPOST_CLI_TAG`/`OPENPOST_CLI_DIR`/`OPENPOST_CLI_BIN`/`OPENPOST_MCP_BIN`. npm trusted-publisher configuration is external.                                                                                                                | The tag workflow packs and publishes `@getopenpost/cli` through trusted publishing after `publish-release` flips the GitHub release public, since the wrapper installs its binaries from that release. Follow `docs/agents/npm-packages.md`.                                                                                                                                                                                                     | `scripts/npm-package-release.mjs` matches the packed tarball integrity to npm. `scripts/verify-published-npm-package.mjs --kind cli` checks registry integrity, npm signatures, the shipped bins, and a live verified install once the pinned release is public. npm provenance records the workflow source. The package SemVer is independent of the app tag. |

Keep external deployment records outside the repository. Do not put Cloudflare responses, credentials, signing material, or operator evidence in Git.

Refresh product screenshots and landing detail crops deliberately through `bun run capture:product-screenshots` when public product imagery changes, never as a release gate. The capture builds the current app and fails loudly if a product flow cannot render. Review generated images before committing; public builds copy them through the asset surface manifest.
